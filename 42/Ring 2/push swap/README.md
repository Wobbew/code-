*This project has been created as part of the 42 curriculum by wobbe.*

## Description

push_swap is a 42 project that implements a sorting program using two stacks and a limited set of operations. The goal is to sort a list of integers (ascending) while producing the smallest possible sequence of operations. This repository contains a C implementation that parses input, builds two stacks, and uses algorithmic strategies to minimize the number of operations required to sort the numbers.

Key points:
- Input: list of integers passed as program arguments (or quoted string arguments).
- Output: the program prints a sequence of stack operations (one per line) that, when applied to the initial stack, will result in a sorted stack.
- Constraints: only allowed stack operations must be used (sa, sb, ss, pa, pb, ra, rb, rr, rra, rrb, rrr).

## Instructions

Build

1. Ensure you have a POSIX-compatible C toolchain installed (gcc/clang, make).
2. At the repository root run:

```bash
make
```

This will build the `push_swap` binary (and helper test artifacts provided in the repo).

Usage

- Basic usage:

```bash
./push_swap 3 2 1 0
```

- You can also pass a single quoted string with space-separated integers:

```bash
./push_swap "2 1 0"
```

- There is a `checker_linux` binary in the repo which can validate the sequence of operations produced by `push_swap`. Example:

```bash
# Generate operations and validate with checker
./push_swap 3 2 1 | ./checker_linux 3 2 1
```

Notes

- The repo includes helper scripts and a tester (`push_swap_test_linux.sh`, `push_swap_tester.py`) to exercise and compare outputs.
- If `make` fails on your system, check `Makefile` and your compiler toolchain. The project expects a standard C environment.

## Project structure (selected files)

- `main.c`, `sort.c`, `stack1.c`, `operations1.c`, `utils.c` — core program logic.
- `Pushswap.h` — project header and types.
- `checker_linux` — validation tool included in repo.
- `push_swap_test_linux.sh`, `push_swap_tester.py` — test scripts.
- `libft/` — utility library used by the project.

## Resources

- 42 push_swap subject (your school's subject page) — consult the official subject for precise rules and evaluation criteria.
- Stack-based sorting overview: https://en.wikipedia.org/wiki/Sorting_algorithm
- Typical strategies and writeups for push_swap: blog posts and GitHub repos from peers (search "push_swap algorithm")
- C standard library documentation (man pages) and your compiler docs for build details.

AI usage

- AI assistance (ChatGPT) was used to draft and format this `README.md` file and to suggest wording and examples for the Instructions and Resources sections. No production code was generated by the AI in this file; the AI contribution was limited to the documentation text.

If you'd like, I can update the first line to include other project authors' 42 logins (e.g., `name1, name2`) — tell me the exact logins to include.

## License / Credits

This repository contains student project code produced as part of the 42 curriculum.

---

If you want additional sections (detailed operation examples, performance notes, or a short explanation of the implemented algorithm), tell me which section and I'll add it.
